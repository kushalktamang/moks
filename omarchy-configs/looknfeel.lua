-- Change the default Omarchy look'n'feel.
hl.config({
   general = {
      border_size = 0,
   },
})

hl.config({
   decoration = {
      -- Use round window corners.
      rounding = 2,
      -- disabling transparent background more then 1 will work
      active_opacity = 1.5,
      inactive_opacity = 1.5,
      fullscreen_opacity = 1.5,
   },
})